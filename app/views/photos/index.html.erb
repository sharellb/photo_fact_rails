<h1>Gallery</h1>
    <% @photos.each do |photo| %>
      <div class="gallery">
          <%= link_to image_tag(photo.image), "photos/#{photo.id}" %>
        </div>
    <% end %>
<br>
<%= link_to 'New Photo', new_photo_path %>
